apiVersion: v1
kind: Service
metadata:
  name: boilerplate-frontend
  labels:
    app: boilerplate-frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: fe-port
      protocol: TCP
      name: fe-lb
  selector:
    app: boilerplate-frontend

---

apiVersion: v1
kind: ConfigMap
metadata:
  name: be-config
data:
  be.url: aba30238cb1bd11e899300a2fa58a648-1226704682.us-east-1.elb.amazonaws.com

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: boilerplate-frontend
  labels:
    app: boilerplate-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: boilerplate-frontend
  template:
    metadata:
      labels:
        app: boilerplate-frontend
    spec:
      containers:
        - name: boilerplate-frontend
          image: 125181119024.dkr.ecr.us-east-1.amazonaws.com/boilerplate:v13
          env:
            - name: BE_URL
              valueFrom:
                configMapKeyRef:
                  name: be-config
                  key: be.url
          ports:
            - name: fe-port
              containerPort: 3000
              protocol: TCP